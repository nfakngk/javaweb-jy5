package com.itdr.cookie1;

/*
@WebServlet(name = "Cookiedemo1",value = "/qwe")
        public class Cookiedemo1 extends HttpServlet {
            protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

    }

    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
        //键值对，一个名字已给值（构造方法）
        //创建一个cookie对象
        Cookie c=new Cookie("u","123,456");
        User sc=new User();
        sc.setName("qweasd");
        sc.setPsa(123);
        Cookie c1=new Cookie("u","123,456");
        //设置cookie在客户端的持久化时间（可以不设置
         //看文档就可以解决
        //c.setMaxAge();
        //设置cookie的携带路径（可以不设置
         //c.setPath("/qwe");用户免登陆。
        //向客户端发送cookie(属于给客户端一个响应)
        response.addCookie(c);

        //删除客户端的cookie

        //通过request获取Cookie

        //遍历Cookie数组，通过Cookie的名称获得我们想要的cookie

    }
}
*/
